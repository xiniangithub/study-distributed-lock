<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wez.study.single.lock.dal.OrderItemMapper">

    <insert id="insertSelective">
        insert into t_order_item (order_id, product_id, purchase_price, purchase_num, create_user, create_time, update_user, update_time)
        values (#{orderId}, #{productId}, #{purchasePrice}, #{purchaseNum}, #{createUser}, #{createTime}, #{updateUser}, #{updateTime})
    </insert>
</mapper>